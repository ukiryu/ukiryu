---
ukiryu_schema: '1.0'
name: tar
interface: tar
implementations:
- name: default
  display_name: Tar
  detection:
    executable: tar
    command: ["--version"]
    pattern: tar ([\d.]+)
  version_scheme: semantic
  versions:
  - equals: '1.0'
    file: 1.0.yaml
  default: 1.0.yaml
