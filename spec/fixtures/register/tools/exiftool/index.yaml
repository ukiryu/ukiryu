---
ukiryu_schema: '1.0'
name: exiftool
interface: exiftool
implementations:
- name: default
  display_name: Exiftool
  detection:
    pattern: exiftool ([\d.]+)
  version_scheme: semantic
  versions:
  - equals: '1.0'
    file: 1.0.yaml
  default: 1.0.yaml
