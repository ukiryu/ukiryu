---
ukiryu_schema: '1.0'
name: gzip_gnu
interface: gzip
implementations:
- name: default
  display_name: Gzip Gnu
  detection:
    executable: gzip
    command: ["--version"]
    pattern: gzip ([\d.]+)
  version_scheme: semantic
  versions:
  - equals: '1.0'
    file: 1.0.yaml
  default: 1.0.yaml
