---
ukiryu_schema: '1.0'
name: jq
interface: jq
implementations:
- name: default
  display_name: Jq
  detection:
    pattern: jq ([\d.]+)
  version_scheme: semantic
  versions:
  - equals: '1.0'
    file: 1.0.yaml
  default: 1.0.yaml
