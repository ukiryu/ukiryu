---
ukiryu_schema: '1.0'
name: sort
interface: sort
implementations:
- name: default
  display_name: Sort
  detection:
    pattern: sort ([\d.]+)
  version_scheme: semantic
  versions:
  - equals: '1.0'
    file: 1.0.yaml
  default: 1.0.yaml
