---
ukiryu_schema: '1.0'
name: ffmpeg
interface: ffmpeg
implementations:
- name: default
  display_name: Ffmpeg
  detection:
    pattern: ffmpeg ([\d.]+)
  version_scheme: semantic
  versions:
  - equals: '1.0'
    file: 1.0.yaml
  default: 1.0.yaml
